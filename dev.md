// 运行所有项目
npm run dev

// 将dev目录index.html打包为web应用
npm run build

// 将packages下所有包打包为web应用
npm run build:app

// 将packages下指定包打包为web应用
npm run build:app name1,name2

// 将packages下所有包打包为npm包
npm run build:lib

// 将packages下指定包打包为npm包
npm run build:lib name1,name2